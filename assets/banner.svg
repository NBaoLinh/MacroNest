<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 700 150" width="700" height="150">
  <defs>
    <!-- Background Gradient matching the app icon background -->
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#052e16"/>
      <stop offset="100%" stop-color="#22c55e"/>
    </linearGradient>
    
    <!-- Glow Gradient for crosshair details -->
    <linearGradient id="glow" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#dcfce7"/>
    </linearGradient>
    
    <!-- Text Nest Gradient to make it look premium (from light-greenish mint to crisp white/light mint) -->
    <linearGradient id="text-nest" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#a7f3d0"/>
    </linearGradient>
  </defs>
  
  <style>
    .title-text {
      font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, 'Inter', Roboto, sans-serif;
      font-weight: 900;
      font-size: 76px;
      letter-spacing: 4px;
    }
    .part-macr {
      fill: #ffffff;
    }
    .part-nest {
      fill: url(#text-nest);
      opacity: 0.95;
    }
    .hud-line {
      stroke: #ffffff;
      stroke-width: 2;
      fill: none;
      opacity: 0.3;
    }
    .axis-line {
      stroke: url(#glow);
      stroke-width: 2;
      opacity: 0.25;
    }
  </style>

  <!-- Banner Background (rounded rect using the icon's gradient background) -->
  <rect width="700" height="150" rx="24" fill="url(#bg)"/>

  <!-- Futuristic HUD corner elements -->
  <path d="M 25 45 L 25 25 L 45 25" class="hud-line" />
  <path d="M 675 45 L 675 25 L 655 25" class="hud-line" />
  <path d="M 25 105 L 25 125 L 45 125" class="hud-line" />
  <path d="M 675 105 L 675 125 L 655 125" class="hud-line" />

  <!-- Horizontal Axis Guide Line (behind the text) -->
  <line x1="100" y1="76" x2="600" y2="76" class="axis-line" />

  <!-- Centered content group -->
  <g transform="translate(82, 0)">
    <!-- Tech Text Group (no mask to avoid spiky artifacts on diagonal letters) -->
    <!-- "MACR" Text -->
    <text x="0" y="105" class="title-text part-macr">MACR</text>
    <!-- "NEST" Text -->
    <text x="321" y="105" class="title-text part-nest">NEST</text>
    
    <!-- White Crosshair (representing the 'O') -->
    <!-- Positioned centered vertically and aligned with the text baseline -->
    <svg x="230" y="26" width="100" height="100" viewBox="0 0 128 128">
      <circle cx="64" cy="64" r="28" fill="none" stroke="#dcfce7" stroke-width="8"/>
      <circle cx="64" cy="64" r="9" fill="#ffffff"/>
      <path d="M64 20 v26 M64 82 v26 M20 64 h26 M82 64 h26" stroke="url(#glow)" stroke-width="10" stroke-linecap="round"/>
      <circle cx="64" cy="64" r="38" fill="none" stroke="url(#glow)" stroke-width="4" opacity="0.9"/>
    </svg>
  </g>
</svg>
